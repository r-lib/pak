
name: pak build, windows-2016, R 3.5

on:
  workflow_dispatch:

jobs:
  pak:
    runs-on: windows-2016

    env:
      R_REMOTES_NO_ERRORS_FROM_WARNINGS: true

    steps:
    - uses actions/checkout@v1
    - uses: r-hub/actions/pak-builder@feature/pak-builder
      with:
        rversions: "3.5"
